name: Print Message
on:
  push:
   branches:
     -main


jobs:
  say-hello:
  runs-on: ubuntu-latest

  steps:
   - name: Print a Message
     run: echo "Hello! A push detected on main branch"

   - name: Print a Second Message
     run: echo "Hello! A Second Push detected on main branch" 




     